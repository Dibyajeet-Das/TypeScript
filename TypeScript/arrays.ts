//arrays



const array1 : string[] = [];

//we can define in this als array
const num : Array<number> = [];

type User = {
    name:String,
    place: String ,
    vote: boolean
}

const Adding : User[] = [];

Adding.push({name:"Nora", place:"Mumbai", vote:false});
console.log(Adding);
array1.push("Hello");
num.push(82);